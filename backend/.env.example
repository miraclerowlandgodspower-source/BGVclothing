# Local setup only. Copy to .env and use your own credentials.
# On Vercel, set these in the project's Environment Variables instead.
MONGODB_URI=
PORT=3000
PAYSTACK_SECRET_KEY=
FRONTEND_URL=http://localhost:5500
